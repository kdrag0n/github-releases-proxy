{
  "name": "github-releases-proxy",
  "version": "1.0.0",
  "description": "GitHub Releases asset proxy for Cloudflare Workers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Danny Lin <danny@kdrag0n.dev>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
